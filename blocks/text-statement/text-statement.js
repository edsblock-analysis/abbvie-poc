/**
 * Text Statement Block — Default content styling block
 * Minimal JS; decoration is handled via CSS
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // No DOM transformation needed; CSS handles all styling
}
