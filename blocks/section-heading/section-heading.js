/**
 * Section Heading Block — Default content styling (CSS only)
 * Minimal JS decoration
 */
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {
  // No DOM transformation needed; CSS handles all styling
}
